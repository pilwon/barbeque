/*
 * admin/static/js/node_modules/jquery.center.js
 */

'use strict';

var $ = require('jquery');

$.fn.center = function () {
  this.css('position', 'absolute');
  this.css('top', Math.max(
    0,
    (($(global).height() - $(this).outerHeight()) / 2) + $(global).scrollTop()) + 'px'
  );
  this.css('left', Math.max(
    0,
    (($(global).width() - $(this).outerWidth()) / 2) + $(global).scrollLeft()) + 'px'
  );
  return this;
};

// Public API
exports = module.exports = $.fn.center;
